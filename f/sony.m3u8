
    <script>
        fetch('sonyplaylist.json')
            .then(response => response.json())
            .then(data => {
                const streamUrl = data.stream;
                const videoPlayer = document.getElementById('my-video');
                videoPlayer.src = streamUrl;
            })
            .catch(error => console.error('Error:', error));
    </script>
